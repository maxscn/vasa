# @vasa/render-test

Pixel comparison utilities for validating Vasa renderer output.

## Development

- Install dependencies:

```bash
vp install
```

- Run the unit tests:

```bash
vp test
```

- Build the library:

```bash
vp pack
```
