# @skriva/render-test

Pixel comparison utilities for validating Skriva renderer output.

## Development

- Install dependencies:

```bash
vp install
```

- Run the unit tests:

```bash
vp test
```

- Build the library:

```bash
vp pack
```
